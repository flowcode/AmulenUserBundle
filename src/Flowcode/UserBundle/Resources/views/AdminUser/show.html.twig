{% extends 'FlowcodeDashboardBundle:Default:layout.html.twig' %}

{% block body -%}
{{ include('FlowcodeUserBundle:AdminUser:usersHeader.html.twig') }}

<section class="content">

    <div class="box box-solid">
        <div class="box-header">
            <div class="box-tools">
                <a class="btn btn-link btn-sm" href="{{ path('admin_user_new') }}">
                    <i class="fa fa-plus"></i> {{ 'create'|trans }}
                </a>
                <a class="btn btn-link btn-sm" href="{{ path('admin_user') }}">
                    Back to the list
                </a>
                <a class="btn btn-link btn-sm" href="{{ path('admin_user_edit', { 'id': entity.id }) }}">
                    Edit
                </a>
            </div>
        </div>
        <div class="box-body">
            <table class="table table-condensed">
                <tbody>
                    <tr>
                        <th>firstname</th>
                        <td>{{ entity.firstname }}</td>
                    </tr>
                    <tr>
                        <th>Email</th>
                        <td>{{ entity.email }}</td>
                    </tr>

                    <tr>
                        <th>Username</th>
                        <td>{{ entity.username }}</td>
                    </tr>
                    <tr>
                        <th>status</th>
                        <td>
                            {% if entity.status %}
                            {{ "active"|trans }}
                            {% else %}
                            {{ "active"|trans }}
                            {% endif %}
                        </td>
                    </tr>
                    <tr>
                        <th>Created</th>
                        <td>{{ entity.created|date('Y-m-d H:i:s') }}</td>
                    </tr>
                    <tr>
                        <th>Updated</th>
                        <td>{{ entity.updated|date('Y-m-d H:i:s') }}</td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>

</section>

{% endblock %}
